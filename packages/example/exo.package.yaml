name: example
description: Various example actions to return friendly greetings
config:
  "$import": "schema/config.schema.json"
actions:
  hello-bash:
    description: My cool Hello world example in Bash
    handler: hello-bash/handler.sh
    # interpreter: bash
    input:
      "$import": "schema/input.schema.json"
    output:
      "$import": "schema/output.schema.json"
  hello-php:
    "$import": "hello-php/exo.action.json"