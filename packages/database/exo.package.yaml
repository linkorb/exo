name: database
description: Actions to interact with databases
config:
  "$import": "config.schema.json"
actions:
  query:
    description: Run a query
    handler: query/query.php
    interpreter: php
    input:
      "$import": "query/input.schema.json"
    output:
      "$import": "query/output.schema.json"