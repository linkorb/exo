{
  "type": "object",
  "required": [ "greeting", "name" ],
  "properties": {
    "greeting": {
      "type": "string",
      "description": "Type of greeting",
      "default": "Hello"
    },
    "name": {
      "type": "string",
      "description": "Name to greet (i.e. 'Joe')"
    }
  }
}
